channels:
  - nodefaults
  - bioconda
  - conda-forge
dependencies:
  - python = 3.8
  - pip
  - abnumber == 0.2.7
  - pip:
    - click >= 7
    - pandas
    - sqlalchemy
    - flask
    - redis
    - celery
    - biopython
    - pytest
    - requests
    - tqdm
    - xlsxwriter
    - humanize
    - fairseq == 0.10.2
